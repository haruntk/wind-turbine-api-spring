package com.windturbine.wind_turbine_api.domain.enums;

/**
 * Anomaly severity levels — derived from the ratio of
 * reconstruction error to threshold.
 */
public enum Severity {
    LOW,
    MEDIUM,
    HIGH
}
